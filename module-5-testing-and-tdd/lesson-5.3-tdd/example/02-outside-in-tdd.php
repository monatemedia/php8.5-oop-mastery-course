<?php
declare(strict_types=1);

/**
 * MOVED — this file now lives at:
 *     module-5-testing-and-tdd/lesson-5.3-tdd/examples/02-outside-in-tdd.php
 *
 * Lesson 5.3 was the only lesson whose folder was named `example/` (singular)
 * while every other lesson — and every README reference — uses `examples/`.
 * The content has been moved to `examples/` to match.
 *
 * This stub is left behind only because the folder could not be deleted
 * automatically. It is safe to delete the whole `example/` folder.
 */

fwrite(STDERR, "This file has moved to examples/02-outside-in-tdd.php\n");
exit(1);
